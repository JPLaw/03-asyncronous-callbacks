'use strict';

const reader = require('../lib/reader.js');

describe(), function() {
    it ('should return an array', function (){
        return
    }   if(err) console.log(err);
        expect();
        });
    });

    it ('should return a string', function (){
        return
        if(err) console.log(err);
        expect();
        });
    });

    it ('should return strings in some order', function(){
        return 
        if(err) console.log(err);
        expect();
        });
    });

    it ('shoudl throw error', function(){
        return
        if(err) console.log(err);
        expect().toThrowError();
    });
});